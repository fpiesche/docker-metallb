FROM golang:alpine AS builder

ADD ./metallb /metallb
WORKDIR /metallb/speaker
RUN go build .

FROM alpine:3.14.2
COPY --from=builder /metallb/speaker/speaker /usr/bin/metallb-speaker
CMD [ "/usr/bin/metallb-speaker" ]
